# Voyage Analytics Production Layer Dependencies

# Web framework
flask==3.0.3

# Model loading
joblib==1.4.2
xgboost==2.0.3

# Machine learning (required by both the flight-price pipeline and the
# gender classification model). Kept as a lower bound so the bundled model
# artifact (serialised with an earlier scikit-learn) still loads cleanly.
scikit-learn>=1.5.0

# Validation
pydantic==2.7.4

# Data handling
pandas==2.2.2

# HTTP client (used by the Streamlit dashboard to call the API)
requests==2.32.3

# Dashboard
streamlit==1.36.0

# Testing
pytest==8.2.2
pytest-cov==5.0.0

# Development
python-dotenv==1.0.1

# Type checking
mypy==1.10.1
